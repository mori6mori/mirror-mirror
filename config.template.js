// Template configuration file - RENAME TO config.local.js and add your API keys
// This is just a template - the actual config.local.js file should never be committed

const CONFIG = {
  PERPLEXITY_API_KEY: "your-perplexity-api-key-here"
};

export default CONFIG;
